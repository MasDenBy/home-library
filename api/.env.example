# Environment
NODE_ENV=production
# The port number for API
PORT=0000
# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER_NAME=user
DB_USER_PASSWORD=password
DB_NAME=db-name
# Logging configuration
LOG_FILE=logs/api.txt
LOG_LEVEL=debug # emerg, alert, crit, error, warning, notice, info, debug